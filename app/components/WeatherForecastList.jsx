/* global require */
var React = require('react');

export var WeatherForecastList = React.createClass({
    render: function () {
        return (
            <div>
                <h4>Forecast data for a future dates will be contained in this list</h4>
            </div>
        )
    }
});

export default WeatherForecastList;
